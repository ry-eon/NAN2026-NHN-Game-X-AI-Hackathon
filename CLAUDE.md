# CLAUDE.md — NAN 2026 해커톤 프로젝트

## ⚡ 세션 시작 시 필독
**현재는 v5 「마지막 성벽」 — 3D 농성전이다.** 2D 그리드 게임(v1~v4)은 사용자 지시로 폐기되어 `legacy-2d` 브랜치에 보존됨. main의 `core/ bots/ pipeline/ client/src/`는 레거시(미사용)다.
**작업 재개 전 `docs/08-status-v5.md`를 먼저 읽을 것** — 현황, 사용자 확정 사항(재론 금지), 남은 일 순서가 거기 있다.

## 프로젝트 한 줄 요약
성주가 되어 3D 성을 직접 걸어다니며 괴수 침공에 맞서는 농성전 게임.
AI가 만든 콘텐츠를 봇 시뮬레이션이 검증하여 통과분만 출고한다.
핵심 논지: **"생성이 아니라 보증"** — AI가 만들었다는 사실이 아니라, 만든 것이 게임으로 성립함을 시스템이 증명한다.

- 대회: NHN NAN 2026 (게임×AI 해커톤, 채용 연계형)
- 사전과제 마감: **2026-08-10** (마감 후 변경 불가)
- 참가 형태: 개인 1인
- 상세 배경: `docs/01-project-brief.md`

## 기술 스택 (확정)
- Three.js + TypeScript + Vite (v5 3D). 레거시 2D는 Phaser 3 (`legacy-2d` 브랜치)
- 배포: GitHub Pages (심사자가 링크 클릭만으로 플레이). 빌드·테스트는 private인 지금도 매 push마다 돌고, 배포 스텝만 public 전환 후 켜진다
- **제출 리포 (단일 원천)**: `origin` = https://github.com/ry-eon/NAN2026-NHN-Game-X-AI-Hackathon
  → Pages 주소: https://ry-eon.github.io/NAN2026-NHN-Game-X-AI-Hackathon/
  (2026-08-01 확정. 과거 `nan2026-Game-x-AI-Hackathon` 리포에 7/25 단 1회 push된 흔적이 있었으나 미사용 — 로컬 원격에서 제거함.
  **다른 원격을 추가하지 말 것**; 리포 이름이 곧 Pages 경로라 바뀌면 제출 링크가 바뀐다)
- 패키지 매니저: pnpm
- 런타임에 유료 API/라이선스 사용 금지 (심사 요건). AI 생성은 전부 빌드 타임.

## 리포 구조 (v5 기준)
```
siege/sim/   # 순수 TS 결정론 sim (새 core). 렌더링·DOM 의존 절대 금지.
client/src3d/ # Three.js 렌더러. sim을 구독만 한다. 게임 로직 작성 금지.
docs/        # 기획·설계·디렉팅 로그 (08-status-v5.md = 현황 스냅샷)
core/ bots/ pipeline/ client/src/  # 레거시 2D — 수정 금지, 삭제도 금지
```

## 절대 원칙
1. **sim의 순수성**: 게임 룰은 전부 `siege/sim`에. client는 표현만. 봇과 클라이언트가 같은 sim을 실행해야 "봇이 검증한 판 = 플레이어가 하는 판"이라는 보증이 성립한다. 이것이 프로젝트의 존재 이유다. 성 치수·충돌·높이는 sim의 CASTLE 상수가 단일 원천이다.
2. **결정론**: sim은 시드 고정 RNG만 사용. 같은 (판, 입력 시퀀스, 시드)면 항상 같은 결과. `Math.random()` 직접 호출 금지.
3. **커밋 기록 = 제출물**: 커밋 히스토리가 심사 대상(요강 명시: "커밋 기록을 유지"). 의미 단위로 커밋하고, 메시지에 무엇을/왜를 남긴다. 리포는 public.
4. **디렉팅 로그**: 매 작업 세션마다 `docs/logs/YYYY-MM-DD.md`에 주요 프롬프트·지시·반려/재시도 내역을 기록한다. AI 활용 기술 문서(제출물 4)의 원재료다. 빼먹지 말 것.
5. **에셋 출처 즉시 기록**: 외부 에셋(이미지·사운드·폰트) 사용 시 `docs/asset-licenses.md`에 출처와 라이선스를 그 자리에서 기록 (제출 요건).
6. **스코프 규율**: 잔여 작업 우선순위는 `docs/08-status-v5.md` §5 참조 (M2b 전투가 최우선). `docs/04-milestones.md`는 2D 시절 기준으로 낡음 — 재작성 전까지 참조 금지. 일정 판단이 애매하면 기능을 줄이는 쪽으로.

## 게임 룰
`docs/02-game-design.md` 참조(일부 2D 시절 기준). v5에서 사용자가 이미 확정한 사항은 `docs/08-status-v5.md` §3의 표가 우선 — 재론하지 말 것. 미결 항목은 임의로 확정하지 말고 사용자와 논의할 것.

## 제출물 체크리스트 (요강 요약)
1. 웹 빌드(GitHub Pages 링크) + 전체 소스 + 커밋 기록
2. 플레이 영상 30~60초, **실제 플레이 화면만** (AI 합성·조작 불가) → YouTube
3. 게임 소개서 PDF (제목·한줄소개·게임방법·실행방법·링크)
4. AI 활용 기술 문서 PDF (구조 설명, 주요 프롬프트·지시사항, 외부 에셋/오픈소스 출처)
5. (개인 참가라 팀원 롤 기술서 생략)
