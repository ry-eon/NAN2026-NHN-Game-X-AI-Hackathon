# CLAUDE.md — NAN 2026 해커톤 프로젝트

## 프로젝트 한 줄 요약
서브컬처 캐릭터 수집형 디펜스/오펜스 하이브리드 게임.
AI 파이프라인이 스테이지와 캐릭터를 생성하고, 봇 시뮬레이션이 검증하여 통과분만 출고한다.
핵심 논지: **"생성이 아니라 보증"** — AI가 만들었다는 사실이 아니라, 만든 것이 게임으로 성립함을 시스템이 증명한다.

- 대회: NHN NAN 2026 (게임×AI 해커톤, 채용 연계형)
- 사전과제 마감: **2026-08-10** (마감 후 변경 불가)
- 참가 형태: 개인 1인
- 상세 배경: `docs/01-project-brief.md`

## 기술 스택 (확정)
- Phaser 3 + TypeScript + Vite
- 배포: GitHub Pages (심사자가 링크 클릭만으로 플레이)
- 패키지 매니저: pnpm
- 런타임에 유료 API/라이선스 사용 금지 (심사 요건). AI 생성은 전부 빌드 타임.

## 리포 구조 (필수 준수)
```
core/      # 순수 TS 시뮬레이션. 렌더링·DOM·Phaser 의존 절대 금지. 결정론적.
client/    # Phaser 렌더러. core를 구독만 한다. 게임 로직 작성 금지.
bots/      # core를 헤드리스로 구동하는 봇 플레이테스터
pipeline/  # 스테이지/캐릭터 생성 → 봇 검증 → 출고 스크립트
docs/      # 기획·설계·디렉팅 로그
```

## 절대 원칙
1. **core의 순수성**: 게임 룰은 전부 core에. client는 표현만. 봇과 클라이언트가 같은 core를 실행해야 "봇이 검증한 스테이지 = 플레이어가 하는 스테이지"라는 보증이 성립한다. 이것이 프로젝트의 존재 이유다.
2. **결정론**: core는 시드 고정 RNG만 사용. 같은 (스테이지, 입력 시퀀스, 시드)면 항상 같은 결과. `Math.random()` 직접 호출 금지.
3. **커밋 기록 = 제출물**: 커밋 히스토리가 심사 대상(요강 명시: "커밋 기록을 유지"). 의미 단위로 커밋하고, 메시지에 무엇을/왜를 남긴다. 리포는 public.
4. **디렉팅 로그**: 매 작업 세션마다 `docs/logs/YYYY-MM-DD.md`에 주요 프롬프트·지시·반려/재시도 내역을 기록한다. AI 활용 기술 문서(제출물 4)의 원재료다. 빼먹지 말 것.
5. **에셋 출처 즉시 기록**: 외부 에셋(이미지·사운드·폰트) 사용 시 `docs/asset-licenses.md`에 출처와 라이선스를 그 자리에서 기록 (제출 요건).
6. **스코프 규율**: 디펜스가 완성되기 전에 오펜스 코드를 작성하지 않는다. 마일스톤은 `docs/04-milestones.md` 참조. 일정 판단이 애매하면 기능을 줄이는 쪽으로.

## 게임 룰
`docs/02-game-design.md` 참조. 미결 항목(세계관 톤, 난이도 지표 세부 정의)은 해당 문서에 표시되어 있음 — 임의로 확정하지 말고 사용자와 논의할 것.

## 제출물 체크리스트 (요강 요약)
1. 웹 빌드(GitHub Pages 링크) + 전체 소스 + 커밋 기록
2. 플레이 영상 30~60초, **실제 플레이 화면만** (AI 합성·조작 불가) → YouTube
3. 게임 소개서 PDF (제목·한줄소개·게임방법·실행방법·링크)
4. AI 활용 기술 문서 PDF (구조 설명, 주요 프롬프트·지시사항, 외부 에셋/오픈소스 출처)
5. (개인 참가라 팀원 롤 기술서 생략)
