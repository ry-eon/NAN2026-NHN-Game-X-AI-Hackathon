# pipeline/

스테이지/캐릭터 생성 → 봇 검증 → 출고 스크립트 (Node, 빌드 타임 전용).

- 단계: 생성 → 스키마 검증 → 봇 시뮬 N회 → 지표 산출 → 판정(출고/반려+사유)
- 모든 판정 결과는 `pipeline/reports/`에 축적 (AI 활용 기술 문서의 증거 자료)
- 유료 API 호출은 여기(빌드 타임)에서만. 런타임에는 생성된 JSON만 포함.

상세: `docs/05-pipeline.md`, W2 마일스톤에서 착수.
