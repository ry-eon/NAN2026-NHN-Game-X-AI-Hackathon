# 디렉팅 로그 — 2026-08-06

## 오늘의 결정과 작업
1. 경쟁작 조사 (GitHub 공개 리포 스캔)
2. **제목 확정: 「최후의 벽, 최후의 사람」**
3. SC식 조작 보강 — 미니맵 · 부대 그룹 확장 · 전군/영웅 선택 키

## 경쟁작 조사

사용자 지시: "해커톤들 요즘 다른 사람들 하는 거 있니? 조사 먼저 해볼래?"
GitHub 검색으로 NAN 2026 사전과제 공개 리포 **약 50개** 확인. 지형은 두 갈래:

- **런타임 AI를 게임 기능으로** (다수파): LLM 에이전트 추리게임(위증), LLM 마피아,
  AI NPC 대화, 규칙 기반 AI Director(NextDir — "Why 패널"로 개입 사유 공개)
- **AI를 개발·검증 파이프라인으로** (우리 진영): domo-ninja-desu가 가장 근접 —
  순수 C# core(엔진 참조 컴파일러 차단) + 헤드리스 sim + 결정론 + Optuna 밸런스 탐색.
  단 조사 시점 기준 "빈 씬" 상태.

판단(사용자 합의): 경쟁 비교는 **제출물에 싣지 않는다**. 조사는 "논지가 아직 유효한가"
확인용으로 종료. 우리 우위는 ①완성 상태(봇 판정 6/6) ②"봇 입력 = 플레이어 입력 =
리플레이 포맷"이라는 구조적 보증 — 기술 문서에서 ②를 또렷하게 민다.

## 제목 확정 — 반려 기록 포함

- AI 1차 제안(성주/마지막 벽 계열, 순성·돌과 살 등) → **사용자 반려**: "한 끗씩 아쉬워",
  "더 후퇴했는데?" — 설명형·영리한 제목이 아니라 **비장함**이 기준임이 드러남.
- 사용자 원안 "최후의 벽, 최후의 사람들"에서 출발, 끝을 단수로 조여
  **「최후의 벽, 최후의 사람」** 확정.
- 도중 사용자 제안 "SEED:ZERO — 이중전선"은 AI가 반려 논거 제시(수성 단일 전선이라
  제목이 게임보다 커짐, 시드 말장난은 기술 문서를 읽어야 성립) → 사용자 수용.
- 파생 제안 "오펜스(지하 탐사) 맛보기 추가"도 반려 합의: 검증 안 된 콘텐츠 출고는
  "봇이 검증한 판만 출고한다"는 논지를 스스로 깬다. D-4 스코프 규율.
- 반영: HUD 좌상단 라벨, `docs/09`·`docs/10` 제목. PDF 표지는 변환 시.

## SC식 조작 보강 (전부 클라이언트 레이어 — sim 불변, 봇 판정 6/6 유효)

사용자 지시: "조작이나 캐릭별 인터페이스를 스타크래프트처럼. 부대 지정, 미니맵, 스킬 편의."

- **미니맵** (`client/src3d/main.ts`): 우상단 190×150 캔버스. 성 윤곽(동벽은 성문 구간을
  비워 그림) + 아군(녹, 하차 병기는 회색)/영웅(금)/성주(백)/적(적, 보스 자색) + 선택 테두리.
  카메라가 성주 고정 추적이라 역할은 화면 점프가 아니라 **전황 파악 + 원격 명령**.
  우클릭 의미는 화면과 동일 — 기존 우클릭 분기를 `issueCommand()`로 추출해 공유
  (조작 규칙이 두 개가 되지 않게).
- **부대 그룹**: Ctrl+1~5 → **Ctrl+1~9 지정 / Shift+1~9 추가 / 1~9 호출**.
  지정은 배타(한 유닛 한 그룹 — SC 관례, 칩 뱃지도 하나만). 선택 패널 칩에 그룹 번호 뱃지.
- **F2 전군 선택**(SC2 관례) · **H 영웅 선택**. 도움말 라인 갱신.

### 확인
- `pnpm typecheck` · `pnpm build` · `pnpm test` 97개 통과 (sim 무변경이므로 당연해야 하고, 실제로 그랬다).
- 브라우저 확인(Chrome MCP): 미니맵 픽셀 검사로 적 138px·아군 140px·영웅·성주 렌더 확인.
  미니맵 우클릭 → 이동 마커(RingGeometry) 씬 추가 확인.
- **교훈 재확인**: 백그라운드 탭은 rAF가 멈춰 입력 래치가 소비되지 않는다 — 처음에
  "성주가 안 움직인다"로 나타났고, 원인은 기능이 아니라 탭 상태였다. 마커 추가로
  핸들러 체인을 검증하고, 래치→sim은 기존 화면 우클릭과 동일 코드임을 근거로 종료.

## 남은 것 (변동 없음 + 오늘 추가)
1. ~~제목 확정~~ ✅
2. 플레이 영상 30~60초 → YouTube (미니맵·부대 지정이 들어간 화면으로)
3. PDF 2종 변환
4. 리포 public 전환 (마지막)
5. 실기기 성능 재측정 (사람만 가능 — 미니맵 캔버스 추가분 포함해서)
6. CI 첫 실행 결과 확인 (`gh` 미설치)
7. **사용자 예고: 1~2일 추가 개발** — 다음 후보는 사용자 지정 대기

## 성능 — "버벅임" 원인 규명과 수정 (같은 날 저녁)

사용자 보고: "내 로컬 PC에서도 너무 버벅거린다. 배포 환경을 다시 봐야 할 것 같다."
호스팅(Pages)은 정적이라 무관 — 문제는 클라이언트 GPU 예산. 감이 아니라 계측부터.

### 계측 (metal GPU headless, docs/08 §4 방법론 · 1080p · 66초 피크 · 구성마다 restart로 동일 틱 재현)

| 구성 | fps | calls | tris |
|---|---|---|---|
| 스케일 1.0 고정 | 42.6 | 971 | 254만 |
| 스케일 0.7 고정 | 59.8 | 1000 | 254만 |
| 0.7 + 그림자 off | 54.8 | 509 | 126만 |
| 0.7 + 블룸 off | 59.7 | 985 | 254만 |

→ **fill-rate bound.** 그림자 패스는 드로우콜의 절반(982→509)을 차지하지만 이 GPU에선
fps 병목이 아니다 (UHD 630처럼 콜 민감한 GPU에선 다시 잴 것).

### 진범: 동적 해상도 조절기의 헌팅

Retina(DPR 2) 20초 추적: 스케일 0.7↔0.85 무한 왕복, **렌더 타깃 재할당 ~10회**, fps 41~60 널뛰기.
매 재할당이 그 프레임의 히치 → 체감 "버벅임"은 낮은 평균 fps가 아니라 **이 진동**이었다.
1차 분해 계측은 판이 실시간으로 흘러 적 수가 21→6으로 줄고 조절기가 강제값을 덮어써 오염 —
restart 재현 + setPixelRatio no-op 패치로 다시 쟀다 (측정 방법도 검증 대상이라는 교훈, 재확인).

### 수정 (`90bb3d2`)
- RES_MAX 1.5 → **1.0** (Retina에서 시작 스케일 1.5 = 2880×1620 fill 폭탄 제거)
- 스케일 변경 쿨다운 (하락 1.5s / 상승 5s) + 상승은 58fps **연속 유지** 시에만
- 재검증: 같은 조건 20초 재할당 0회, 스케일 0.7 정착, fps 55~60

### 배포 전략 정리
- 호스팅은 Pages 유지 (정적, 심사 요건 부합). 성능은 클라이언트 예산 문제로 접근.
- dev 서버(5174) 체감은 참고만 — 판정은 프로덕션 빌드(preview/docker)로.
- 남김: UHD 630 실기기 재측정(사람), 필요 시 그림자 다이어트(맵 크기·캐스터 축소)가 다음 손잡이.
