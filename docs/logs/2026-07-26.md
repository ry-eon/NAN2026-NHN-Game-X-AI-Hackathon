# 디렉팅 로그 — 2026-07-26

## 오늘의 목표
- M2b 전투 구현 (v5 최우선 항목): 적 침공 실동작 · 아군 유닛 배치/명령 · 전투 판정

## 주요 지시/프롬프트 (기술 문서 발췌용)
- 맥락: 세션 재개. `docs/08-status-v5.md` 기준 M2b(전투 0%)가 최우선.
- 지시: "진행" — 남은 일 1순위(적 침공 실동작부터)를 승인, 세부는 확정 디렉션(§3) 준수 위임.
- 결과: 채택. M2b 1~4(적 실동작·유닛 배치·영웅 유닛·승패/성벽 HP)를 한 세션에 구현.
  - sim 먼저: 아군 4종(궁수/발리스타/대포/영웅) + 부대 명령(unitMove) + 접전/사격/사망.
    성주 이동 로직을 `Mover`로 공용화해 유닛도 계단·2층 지형 규칙을 그대로 탐.
  - 배치는 "초기 자동 배치 + 부대 명령으로 재배치" 방식 채택 — 별도 배치 UI 없이
    스타크래프트식 선택/명령(확정 디렉션)만으로 배치 요건을 충족시키는 스코프 판단.
  - 클라이언트는 표현만: 드래그 박스 선택(화면 투영), 투사체/폭발/사망 FX는
    sim 이벤트(unitFired 등) 소비 — 피해 판정은 전부 sim에서 이미 확정.

## 반려·재시도 기록
- **밸런스 1차 반려(자체)**: AFK 실행에서 성벽 무피해 승리(2000/2000) — 긴장감 0.
  화력 하향(사거리 26→20 등)만으로는 여전히 무피해 → 적 HP 상향(야귀 480→700,
  갑주귀 1400→2300)으로 재시도. 최종: AFK 승리하되 성벽 560/2000(28%) 잔존, 93초.
  "이기긴 하는데 아슬아슬" — 조작 개입(영웅 출격·화력 집중)의 가치가 생기는 지점.
- **headless 검증 1차 실패**: swiftshader가 0.5fps라 실시간 스크린샷에선 sim이
  거의 진행 안 됨(15초 대기에도 괴수 0). 스크린샷 2장이 픽셀 단위 동일해 "렌더 루프
  정지"로 오판할 뻔 — fps 측정으로 원인 분리. 해결: `window.__siege.fastForward(n)`
  검증 훅 추가(결정론이라 빨리감기 안전, FX 이벤트만 버림). 이후 성벽 위 화망·
  W3 성벽 피해 장면 시각 확인 성공.

## 결정 사항
- 전투 룰: 유닛은 정지 상태에서만 사격(SC 공성 관례) / 괴수는 지상 아군(h<1) 접전 우선,
  성벽 위 유닛은 근접 불가(공성전 판타지 유지) / 대포만 AoE.
- 유닛 겹침 분리는 같은 층에서만 + `stepHeight` 검증 — 성벽 밖으로 밀려 떨어지는 것 차단.
- 결정론 유지 장치: 부대 대형 오프셋은 id 정렬 후 배정, 타깃 선정은 최근접(동률 시 낮은 id).
- 테스트가 곧 보증: `siege/test/world.test.ts` — 결정론·무방비 패배·기본 배치 방어 성공
  (시드 20260725 판을 sim 단독으로 단언). 봇 검증 재구축(M3)의 씨앗.

## 세션 2부 — 영웅 스킬 + 상태창 (같은 날 이어서)
- 지시: "다음 진행" — M3 1순위(영웅 스킬 원신식 조준 + 클릭 상태창) 착수 승인.
- 스킬 「업화」 설계 결정(미결이었음 — 구현하며 확정, 수치는 추후 조정 가능):
  지점 지정 광역 화염, 피해 500 / 반경 4.5 / 사거리 18 / 쿨 14초.
  **조준 모드는 클라이언트 보조 기능**으로 설계 — 자동(최다 밀집점)이든 수동(레티클)이든
  sim에 도달하는 건 「시전 지점」 커맨드 하나. 사거리·쿨다운 검증은 sim이 수행.
  → 리플레이 포맷이 조준 UI와 무관해져 봇 검증 논지가 유지된다.
- 상태창: 선택 유닛 우선, 없으면 클릭한 괴수 조사(HP·공격·성벽 파괴·속도).
- 검증: 스킬 sim 테스트 +1(총 69) — 광역 반경·쿨다운·사거리 검증.
  headless 스크린샷으로 수동 조준 레티클·드래그 선택 링·상태창 UI 확인.

## 내일로 넘기는 것
- 적 비주얼이 아직 구체(placeholder) — 괴수 절차 모델 필요 (다크소울 톤 요구와 충돌 중)
- 실기기(GPU) 플레이 확인 — headless는 0.5fps라 조작감·FX 타이밍 검증 불가
- 스킬 밸런스(업화 수치)는 실플레이 후 재조정 여지 있음
- M3 잔여: 봇 검증 재구축(커맨드 리플레이) · 괴수 비주얼 · 캐릭터 퀄리티 결정
