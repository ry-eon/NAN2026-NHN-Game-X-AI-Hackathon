# 디렉팅 로그 — 2026-08-07

## 오늘의 결정과 작업
1. **배포 파이프라인 검증 완료** — CI 첫 결과 확인(성공) + 미push 8/6분 10커밋 push + Pages 하위 경로 로컬 재현 통과
2. **퍼블릭 전환 전 점검** — 히스토리 유출 스캔 깨끗함. "기록 정리 후 퍼블릭" 고민은 **정리 불필요·금지**로 종결
3. **버셀 테스트 배포 신설** (사용자가 UI로 직접 설정) — private 기간의 상시 테스트 URL
4. **듀얼 모니터 가장자리 팬 버그 수정** — 커서가 옆 모니터로 넘어가면 팬이 꺼지던 문제

## 배포 검증 (사용자 지시: "우선 배포쪽부터 봐야하지?")

- `gh` 미설치 상태에서 **`git credential fill`로 저장 토큰을 꺼내 GitHub REST API를 직접 조회** —
  Actions 기록 확인 경로 확보. run #50(8/5 push): 성공, private 조건부(Pages 스텝 skip)도 의도대로.
- **미push 10커밋 발견**(8/6 작업 전부) → 로컬에서 CI와 동일 검증(테스트 99개·빌드·dist 점검) 후
  push → run #51 성공. 이로써 "CI 한 번도 확인 안 됨" 항목 해소.
- **Pages 하위 경로 재현**: `GHPAGES_BASE=/NAN2026-NHN-Game-X-AI-Hackathon/`로 빌드,
  로컬 서버의 같은 하위 경로에서 서빙 → 34파일 전부 200, 번들에 base 우회 절대경로 없음,
  실제 Chrome 부트 검증(리소스 31건 성공·콘솔 에러 0·`__siege` 존재·렌더 정상).
  8/1에 실제로 겪은 하위 경로 404 리스크를 **public 전환 없이 선제 제거**.

## 버셀 테스트 배포 — 프레이밍 교정 기록

- 사용자: "프라이빗이면 우선 버셀로 배포하는 게 나은가?" → AI 1차 답: **불필요**
  (버셀은 루트 서빙이라 하위 경로 리스크를 테스트 못 함, 제출 링크는 Pages).
- 사용자 재프레이밍: **"우리가 배포하고 테스트를 계속 해야 하니까"** — 일회성 검증이 아니라
  남은 기간의 상시 테스트 환경이 논점 → 그 용도로는 **버셀이 정답**으로 채택.
  (교훈: 같은 질문이라도 "검증 수단"과 "테스트 환경"은 답이 다르다)
- 사용자가 버셀 UI로 직접 설정. **https://nan-2026-nhn-game-x-ai-hackathon.vercel.app**
  — main push마다 자동 배포. 부트 검증 통과(리소스 30건 성공·에러 0·최신 하단 바 확인).
- **제출물에는 버셀 주소를 싣지 않는다** — 소개서·기술 문서의 링크는 Pages 주소만.

## "기록 정리 후 퍼블릭" 고민 종결

- 커밋 히스토리 rewrite는 **하지 않는다** — 요강이 "커밋 기록 유지"를 명시, 0/6→6/6 과정
  자체가 "봇이 반려하고 재시도했다"는 논지의 증거.
- 대신 유출 점검 실시: 전 히스토리 시크릿 패턴 스캔(유일 매치 = deploy.yml의
  `id-token: write`, 무해) · 민감 파일명 0건 · 커밋 이메일은 GitHub 계정 주소라 통상 노출.
  → **지금 상태 그대로 public 전환 안전** 판정.

## 듀얼 모니터 가장자리 팬 수정 (`client/src3d/main.ts`)

- 사용자 버그 리포트: "듀얼 모니터라 그런가 마우스 이동이 잘 안되네?"
- 원인: 가장자리 팬은 커서가 화면 끝 18px 띠 안에 있어야 하는데, `mouseleave`가
  `mouseIn=false`로 팬을 꺼버림. 모니터 1대면 OS가 커서를 화면 끝에 붙잡아 문제가 안 되지만,
  듀얼 모니터에선 커서가 경계를 그냥 통과 → **가장 세게 밀고 싶은 순간에 팬이 꺼짐**.
- 수정: `mouseleave`를 중단이 아니라 **"나간 변의 띠로 좌표 고정 → 그 방향 유지"**로 변경
  (창모드 RTS 표준 동작. 브라우저는 커서 confine API가 없음). 멈춤 조건은 `blur`(다른 앱
  클릭)와 커서 복귀뿐. sim 무관 — 결정론·봇 판정 6/6 불변.
- 시각 검증 한계: 자동화 탭은 rAF 정지라 팬 동작을 눈으로 못 잡음(기존 교훈 재확인).
  타입체크·빌드 통과, **실검증은 사용자의 듀얼 모니터 실기기에서 버셀 배포로**.

## 내일로 넘기는 것
- docs/09·10 구 룰 서술 수정(백병전 G·고정 카메라 → 8/6 룰) → PDF 2종 변환
- 플레이 영상 · 실기기 성능 재측정 (사람만 가능)
- 마지막: 리포 public 전환 → Pages 링크 확인
