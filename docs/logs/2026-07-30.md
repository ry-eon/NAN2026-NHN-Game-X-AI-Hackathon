# 디렉팅 로그 — 2026-07-30

## 오늘의 목표
- 그래픽 로드맵 다음 항목: **괴수 모델** (구체 임시 렌더 → 절차 조형 3종)

## 주요 지시/프롬프트 (기술 문서 발췌용)
- 맥락: 세션 시작 시 "현재 어떤 테스크 남아 있니" → 08-status 기준 잔여 목록 제시,
  사용자가 로드맵 합의 순서대로 "괴수 모델" 선택.
- 구현: `models.ts`에 `makeMonster(kind)` + `animateMonster()` 추가.
  기사와 같은 원칙(lathe 프로파일·베벨·부품 조립·단일 프리미티브 금지)이되
  병종이 실루엣으로 읽히게: **야귀**=구부정한 덩치+등뼈 가시+양뿔+징 곤봉,
  **질주귀**=전경 자세+장완 갈퀴손+후방 단일 뿔, **갑주귀**=판금 거구+뿔투구+대형 망치.
  공통 장치: 발광 눈(부감 거리 식별)·발톱 발·피부색은 기존 구체 색상 계승(정보 연속성).
- 렌더 통합: 구체 메시 풀 → 리그 풀(`enemyVisuals`) 교체. 걷기/대기 애니메이션(sim 이동
  여부로 판정), **공격 스윙은 sim 이벤트 구동**(meleeHit/wallHit 시각 기준 윈드업→내리찍기,
  피해는 sim이 이미 적용 — 투사체와 같은 "연출은 그림뿐" 원칙). 접전 시 대상 바라보기(연출 전용).
  피킹은 재귀 레이캐스트+조상 탐색으로 변경(부품 그룹이라).

## 반려·재시도 기록 (자체 검증 루프)
- 1차 스크린샷 검증(Claude in Chrome, 실 GPU): 갑주귀 철갑(MATS.iron 0x33353c)이
  대낮 들판에서 **검은 덩어리로 뭉개짐** — 견갑·투구가 한 실루엣으로 붙어 보임.
  → 전용 MAT_ARMOR(0x565b68)로 교체, 질주귀 피부도 채도 보정(0xb56a30→0x9c4f28).
  2차 스크린샷에서 판금 몰딩이 빛을 받는 것 확인.
- 검증 중 발견한 훅 부작용: `__siege.fastForward`는 이벤트를 버리므로 유닛이 죽어도
  unitDied 연출이 안 돌아 **유령 비주얼**이 남음 → syncScene에 스테일 스윕 추가
  (괴수 풀과 동일 패턴). 실플레이 경로에는 영향 없음.
- 검증 방법 메모: 부감 카메라라 원경 모델 판정이 어려움 → sim을 status 조작으로 동결하고
  괴수 3종을 성벽 앞에 정렬(콘솔 주입) 후 성주를 근처로 보내 근접 촬영. 접전 스윙은
  미끼 유닛을 심어 개활지에서 포착(야귀 오버헤드 윈드업 확인).

## 검증
- `pnpm test` 70/70 통과 (sim 무변경 — 렌더 전용 작업).
- 풀 전투 리플레이(시드 20260725, AFK): 승리·성벽 500/2000·90초 — 08-status 밸런스 수치와 일치.
- 콘솔 에러 0, `pnpm build` 통과. 스폰→전멸까지 리그 생성/제거 누수 없음.
- 실기기 조작감·최대 물량 시 fps는 여전히 사용자 확인 필요 항목(기존 to-do 유지).
  참고: 괴수 1기당 메시 ~15~25개(그림자는 대형 부품만) — wave3 동시 ~10기 수준은
  드로우콜 여유 내로 판단, 1440p 실측은 실기기에서.

## 결정 사항
- 괴수 3종 절차 모델 출고 — 그래픽 로드맵 다음 항목은 **타격감/애니메이션**.
