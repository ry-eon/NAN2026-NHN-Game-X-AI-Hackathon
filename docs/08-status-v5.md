# 08. v5 현황 — 「마지막 성벽」 3D 농성전 (2026-07-25 기준)

> 세션 재개용 스냅샷 + 해커톤 제출 문서(게임 소개서·AI 활용 기술 문서)의 원재료.
> 상세 작업 내역·프롬프트·반려 기록은 `docs/logs/2026-07-25.md` 등 디렉팅 로그 참조.

## 1. 지금 게임이 뭔가

- **나는 성주다**: 플레이어는 성주 캐릭터 하나로 3D 성 내부를 직접 걸어다닌다.
- LoL식 고정 부감 카메라 + 우클릭 이동 + 휠 줌. (WASD 아님 — 사용자 확정)
- 준비 단계에서 성을 둘러보고, Space로 침공 개시 → 괴수들이 **전면 광역**으로 몰려온다 (일자 레인 아님).
- 난이도는 1라운드 단일 고정.
- 프로젝트 논지는 유지: **"생성이 아니라 보증"** — 결정론 sim을 봇이 검증하고 통과분만 출고.
- v1~v4(2D 그리드 디펜스, Phaser)는 **폐기가 아니라 보존**: `legacy-2d` 브랜치 (63+ 테스트, 봇 3종, 파이프라인 포함). main은 3D 재출발 (`15526aa`부터).

## 2. 코드 구조 (v5)

```
siege/sim/world.ts     # 순수 TS 결정론 sim — 렌더 의존 0. 이것이 새 "core"
client/src3d/main.ts   # Three.js 부트스트랩·카메라·입력·보간 렌더 루프
client/src3d/environment.ts  # 성·지형·하늘·소품 (절차 생성 + CC0 에셋)
client/src3d/models.ts # 절차 캐릭터(풀아머 기사)·화염 FX
client/index.html      # 엔트리 → /src3d/main.ts
core/ bots/ pipeline/  # 레거시 2D (main에도 남아있지만 미사용)
```

### sim 핵심 (siege/sim/world.ts)
- 30Hz 고정 틱, 연속 XZ 좌표, mulberry32 시드 RNG. 입력은 커맨드형(`moveTo`) = 그대로 리플레이 포맷.
- **CASTLE 상수가 단일 원천**: `{east:-6, west:-30, north:-18, south:18, wallH:11, wallT:8, gateHalf:3.2}` — 충돌·높이·렌더가 전부 이걸 공유. 렌더와 sim이 어긋나면 "봇이 검증한 판 = 플레이어가 하는 판" 보증이 깨지므로 여기서만 수정할 것.
- **2층 지형**: `heightLevels(x,z): number[]` — 성문 스트립은 `[11, 0]` (위 다리 + 아래 터널 공존). `stepHeight()`가 Δ≤1.5 연속 전이만 허용(절벽 차단). `findPath()`는 노드=셀×2+층 BFS, 클릭 면 높이가 목표 층 힌트.
- **보도는 4면 전체 개방** (모서리 연속). 내성 관통 구간(x∈[west+1.5, west+10.5], z∈[±6.5])만 차단.
- 계단: 동벽 안쪽 z∈±[4.5,15] 경사로(0→11).

### 렌더 핵심 (client/src3d/)
- Three.js 0.185: ACES 톤매핑, HDRI 환경광, PBR(변위 포함), EffectComposer(SSAO+Bloom+비네트).
- 30Hz sim ↔ 60fps 렌더: 이전 틱 스냅샷 + alpha 보간. 입력은 래치로 sim 스텝 안에서만 소비(드롭 방지).
- 카메라→성주 엄격 레이캐스트로 가림 벽만 페이드(성주가 성벽 위면 비활성).
- 성가퀴는 **월드 4.2 그리드** 정렬(구간·성문 이음새 간격 균일). 보도 윗면은 포장 데크 4장(벽 박스 윗면 UV 늘어짐 은폐, 본체 0.25 인하로 변위 관통 방지).
- 절차 조형 상한 검증 완료: 풀아머 기사(makeKnight)가 현재 캐릭터 퀄리티 한계선. 실사(다크소울급)는 외부 에셋 필요 — 사용자에게 고지됨.

## 3. 확정된 디렉션 (사용자 결정 사항 — 재론 금지)

| 항목 | 결정 |
|---|---|
| 조작 | LoL식 부감 + 우클릭 이동 + 휠 줌 (WASD 기각) |
| 그래픽 기준 | "다크소울" = **렌더링 퀄리티** 지칭 (분위기·조작 아님). 절차 한계는 고지됨, 외부 에셋은 추후 판단 |
| 밝기 | "조금은 밝게" 반영됨 (노출 1.3, 헤미 1.35) |
| 전투(M2b) | 스타크래프트식 유닛 조작 (드래그 선택 + 우클릭 명령) |
| 필수 스펙 | 영웅·기본 병사·공성 병기(대포/발리스타)·영웅 스킬(원신식 조준, 자동/수동)·상태창 |
| 성벽 | 폭 8 평평한 보도 = 대포·병사 배치 공간. 4면 전체 이동 가능 |
| 라운드 | 1라운드 고정 |
| 리포 | private 유지, 제출 전 public 전환. 커밋 히스토리 = 심사 대상이므로 삭제 금지 |

## 4. 검증 방법론 (해커톤 문서용 핵심 소재)

- **sim 단독 경로 테스트**가 스크린샷보다 우선: `/tmp/pathtest2.ts` (tsx로 실행) — 계단 등벽·성문 터널·성문 위 보도 직행·4면 순환을 렌더 없이 단언. 절대경로 import라 리포 밖. 재생성 필요하면 이 문서의 이력 참조.
- 시각 검증은 headless Chrome(playwright-core + swiftshader)으로 첫 로드 기본 화면 스크린샷.
- 교훈 기록(로그에 원문): "성공 스크린샷은 우연일 수 있다 — sim 단언이 먼저", "본체·장식(파라펫)은 별도 레이어라 따로 점검".

## 5. 남은 일 (마감 2026-08-10, D-16)

**최우선 — M2b 전투 (현재 v5에 전투 0%):**
1. 적 침공 실동작 (스폰은 있음 → 이동·성벽 공격·사망)
2. 병사·대포·발리스타 배치 (보도 위 + 지상) — 스타크래프트식 선택/명령
3. 영웅 유닛 (걸어서 이동, 사거리 공격)
4. 승패 판정 + 성벽 HP 연동 (HUD는 있음)

**그 다음 (M3, 필수 스펙):**
5. 영웅 스킬 원신식 조준(자동/수동 토글) + 클릭 시 상태창
6. 봇 검증 재구축: 레거시 봇을 새 sim에 이식 — 커맨드 시퀀스 리플레이로 "보증" 논지 복원
7. 캐릭터 퀄리티 결정: 절차 기사 유지 vs 외부 에셋(Mixamo 등 — 사용자 다운로드 필요)

**제출물 (요강):**
8. 플레이 영상 30~60초 (실제 플레이 화면만, 합성 불가) → YouTube
9. 게임 소개서 PDF / AI 활용 기술 문서 PDF (원재료: 이 문서 + docs/logs/*)
10. 리포 public 전환 + GitHub Pages 확인 (private 동안 배포 워크플로 자동 스킵 중)
11. 게임 제목 확정 (가제 「마지막 성벽」)
12. `docs/04-milestones.md`가 2D 시절 기준으로 낡음 — 재작성 필요

## 6. 다시 시작하는 법

```bash
pnpm install
pnpm dev        # localhost:5173 — 우클릭 이동, 휠 줌, Space 침공
pnpm typecheck && pnpm build
```
- 외부 에셋 추가 시 그 즉시 `docs/asset-licenses.md` 기록 (현재 전부 CC0: Poly Haven HDRI/모델, ambientCG 텍스처).
- 매 세션 `docs/logs/YYYY-MM-DD.md` 디렉팅 로그 필수.
